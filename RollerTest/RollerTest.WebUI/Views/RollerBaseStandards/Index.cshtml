@model IEnumerable<RollerTest.Domain.Entities.RollerBaseStandard>

@{
    ViewBag.Title = "Index";
}

<h2>试验标准</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Standard)
        </th>
        <th>Action</th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Standard)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RollerBaseStandardID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RollerBaseStandardID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RollerBaseStandardID })
        </td>
    </tr>
}

</table>
