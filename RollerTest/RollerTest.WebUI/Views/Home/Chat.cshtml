
@{
    ViewBag.Title = "Chat";
}

<h2>Chat</h2>

<div class="container">
    @*<input type="text" id="message" />*@
    <input type="button" id="sendmessage" value="Send" />
    @*<input type="hidden" id="displayname" />*@
    @*<ul id="discussion"></ul>*@
    <p id="discussion1"> </p>
    <p id="discussion2"> </p>
    <p id="discussion3"> </p>
    <p id="discussion4"> </p>
    <p id="discussion5"> </p>
    <p id="discussion6"> </p>
    <p id="discussion7"> </p>
    <p id="discussion8"> </p>
    <p id="discussion9"> </p>
    <p id="discussion10"> </p>
    <p id="discussion11"> </p>
    <p id="discussion12"> </p>

</div>
@section scripts {
    <!--Script references. -->
    <!--The jQuery library is required and is referenced by default in _Layout.cshtml. -->
    <!--Reference the SignalR library. -->
    <script src="~/Scripts/jquery.signalR-2.2.2.js"></script>
    <!--Reference the autogenerated SignalR hub script. -->
    <script src="~/signalr/hubs"></script>
    <!--SignalR script to update the chat page and send messages.-->
    <script>
        $(function () {
            // Reference the auto-generated proxy for the hub.
            //注册代理服务器
            //var chat = $.connection.chatHub;
            var time = $.connection.timeHub;

            // Create a function that the hub can call back to display messages.
            //代理服务器向所有客户端的addNewMessageToPage传递参数，并实现该方法
            //chat.client.addNewMessageToPage = function (name, message) {
            //    // Add the message to the page.
            //    $('#discussion').append('<li><strong>' + htmlEncode(name)
            //        + '</strong>: ' + htmlEncode(message) + '</li>');
            //};
            time.client.addNewTimeToPage = function (station, time) {
    //            $('#discussion').append('<li><strong>' + htmlEncode(station)
                //+ '</strong>: ' + htmlEncode(time) + '</li>');
                x1 = document.getElementById("discussion1");
                x2 = document.getElementById("discussion2");
                x3 = document.getElementById("discussion3");
                x4 = document.getElementById("discussion4");
                x5 = document.getElementById("discussion5");
                x6 = document.getElementById("discussion6");
                x7 = document.getElementById("discussion7");
                x8 = document.getElementById("discussion8");
                x9 = document.getElementById("discussion9");
                x10 = document.getElementById("discussion10");
                x11 = document.getElementById("discussion11");
                x12 = document.getElementById("discussion12");
                switch (station) {
                    case "1#": x1.innerHTML = station + ":" + time; break;
                    case "2#": x2.innerHTML = station + ":" + time; break;
                    case "3#": x3.innerHTML = station + ":" + time; break;
                    case "4#": x4.innerHTML = station + ":" + time; break;
                    case "5#": x5.innerHTML = station + ":" + time; break;
                    case "6#": x6.innerHTML = station + ":" + time; break;
                    case "7#": x7.innerHTML = station + ":" + time; break;
                    case "8#": x8.innerHTML = station + ":" + time; break;
                    case "9#": x9.innerHTML = station + ":" + time; break;
                    case "10#": x10.innerHTML = station + ":" + time; break;
                    case "11#": x11.innerHTML = station + ":" + time; break;
                    case "12#": x12.innerHTML = station + ":" + time; break;
                }
               
                

            }

            // Get the user name and store it to prepend to messages.
            //$('#displayname').val(prompt('Enter your name:', ''));
            // Set initial focus to message input box.
            //$('#message').focus();
            // Start the connection.
            //代理服务器做的事情，调用了ChatHub.cs上的Send方法
            $.connection.hub.start().done(function () {
                $('#sendmessage').click(function () {
                    //// Call the Send method on the hub.
                    //chat.server.send($('#displayname').val(), $('#message').val());

                    //// Clear text box and reset focus for next comment.
                    //$('#message').val('').focus();
                time.server.send("1#",111111);
            }
                );
            });
        });
        //调用的生成html的方法
        // This optional function html-encodes messages for display in the page.
        function htmlEncode(value) {
            var encodedValue = $('<div />').text(value).html();
            return encodedValue;
        }
    </script>
    }