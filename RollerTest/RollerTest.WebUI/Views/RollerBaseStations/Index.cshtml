@model IEnumerable<RollerTest.Domain.Entities.RollerBaseStation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Device)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Station)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Device)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Station)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RollerBaseStationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RollerBaseStationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RollerBaseStationID })
        </td>
    </tr>
}

</table>
